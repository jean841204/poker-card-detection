# 🃏 撲克牌花色辨識系統

基於 YOLOv7 物件檢測模型的撲克牌花色辨識 Web 應用程式，提供互動式的花色辨識體驗。

## 🌐 線上 Demo

**立即試用**: [https://cybersecurityhw4-jean.streamlit.app/](https://cybersecurityhw4-jean.streamlit.app/)

無需安裝，直接在瀏覽器中體驗完整功能！

## 📊 專案簡介

本專案使用 **YOLOv7** 物件檢測模型進行撲克牌花色辨識。系統能夠辨識撲克牌的四種花色（黑桃♠、紅心♥、方塊♦、梅花♣），適用於各種撲克牌相關應用場景。

### 🎯 模型表現

- **模型架構**: YOLOv7 Object Detection
- **訓練資料集**: 撲克牌花色資料集
- **可辨識類別**: 4 種花色（黑桃、紅心、方塊、梅花）

### 💡 應用場景

本系統可應用於多種場景：

- **遊戲輔助**: 自動辨識撲克牌花色，輔助遊戲進行
- **影片分析**: 從撲克牌遊戲影片中提取花色資訊
- **數據統計**: 統計遊戲中各花色出現的頻率

## ✨ 功能特色

### 1. 📤 自訂圖片上傳
- 支援多種圖片格式（PNG, JPG, JPEG, BMP）
- 即時上傳並進行推論
- 顯示 Top 5 預測結果及信心度

### 2. 🎬 影片上傳與分析（NEW!）
- 支援多種影片格式（MP4, AVI, MOV, MKV）
- 智慧取樣：可調整每 N 幀處理一次，加快處理速度
- 逐幀辨識：對影片中的每一幀進行花色辨識
- 統計分析：自動統計影片中各花色出現的次數與比例
- 進度顯示：即時顯示影片處理進度

### 3. 📁 範例圖片測試
- 內建撲克牌花色範例集（`data/` 資料夾）
- 網格式瀏覽介面，方便快速選擇
- 一鍵測試模型辨識效果

### 4. ⚙️ 彈性參數調整
- **信心度閾值調整**: 控制預測結果的可信度門檻（0.0 - 1.0）
- **圖片尺寸設定**: 調整輸入圖片大小以優化推論速度與準確度（預設 224x224）
- **影片取樣率**: 控制影片處理的幀數取樣間隔
- **顯示結果數**: 限制影片辨識結果的顯示數量

### 5. 📊 詳細結果展示
- 視覺化呈現原始圖片與預測結果
- Top 5 預測類別及對應信心度百分比
- 突出顯示最高信心度的預測結果
- 影片模式下提供完整的統計圖表與分析

## 🚀 快速開始

### 環境需求

- Python 3.8 或以上版本
- 建議使用虛擬環境

### 安裝步驟

1. **Clone 或下載專案**
   ```bash
   cd /path/to/hw4
   ```

2. **（建議）創建虛擬環境**
   ```bash
   python -m venv venv

   # macOS/Linux 啟動虛擬環境
   source venv/bin/activate

   # Windows 啟動虛擬環境
   venv\Scripts\activate
   ```

3. **安裝所需套件**
   ```bash
   pip install -r requirements.txt
   ```

4. **執行 Streamlit 應用程式**
   ```bash
   streamlit run streamlit_app.py
   ```

5. **開啟瀏覽器**

   應用程式會自動在瀏覽器中開啟，預設網址為 `http://localhost:8501`

## 📁 專案結構

```
demo_web/
├── streamlit_app.py       # Streamlit Web 應用程式主程式
├── inference_usps.py      # 原始推論腳本（批次處理）
├── requirements.txt       # Python 套件依賴清單
├── packages.txt           # 系統依賴套件（用於 Streamlit Cloud）
├── README.md             # 專案說明文件
├── weight/
│   └── best.pt           # YOLOv8 訓練好的模型權重檔
└── data/
    └── ...               # 撲克牌花色範例圖片
```

## 🎮 使用說明

### 方法一：上傳自己的圖片

1. 點選 **「📤 上傳圖片」** 分頁
2. 點擊上傳按鈕，選擇一張撲克牌花色圖片
3. 點選 **「🚀 開始辨識」** 按鈕
4. 查看辨識結果及信心度

### 方法二：上傳影片進行分析（NEW!）

1. 點選 **「🎬 上傳影片」** 分頁
2. （選用）展開「影片處理設定」調整取樣率和顯示結果數
3. 點擊上傳按鈕，選擇一個撲克牌遊戲影片
4. 點選 **「🚀 開始辨識影片」** 按鈕
5. 等待影片處理完成（會顯示進度條）
6. 查看逐幀辨識結果和統計分析

### 方法三：使用範例圖片

1. 點選 **「📁 選擇範例圖片」** 分頁
2. 瀏覽顯示的範例圖片縮圖
3. 點擊任一圖片下方的 **「選擇」** 按鈕
4. 自動進行辨識並查看結果

### 調整推論參數

在左側邊欄可以調整：
- **信心度閾值**: 建議值 0.25，數值越高代表模型需要更高的確定性才會輸出結果
- **圖片大小**: 預設 640x640，可調整範圍 320-1280（YOLOv7 標準尺寸）

在影片模式下可以調整：
- **取樣率**: 每 N 幀處理一次（預設 30），數值越大處理速度越快
- **最多顯示結果數**: 限制顯示的幀數（預設 10），避免頁面過長

## 🔧 技術棧

- **深度學習框架**: [YOLOv7](https://github.com/WongKinYiu/yolov7) + PyTorch
- **Web 框架**: [Streamlit](https://streamlit.io/)
- **圖片處理**: Pillow (PIL)
- **影片處理**: OpenCV (cv2)
- **數值計算**: NumPy + Pandas

## 📈 模型訓練資訊

- **模型**: YOLOv7 Object Detection
- **資料集**: 撲克牌花色資料集
  - 類別: 4 種花色（黑桃♠、紅心♥、方塊♦、梅花♣）
- **圖片尺寸**: 640x640 像素（可調整）

## 📝 範例資料說明

`data/` 資料夾中的圖片為撲克牌花色範例集，包含四種花色的樣本。這些圖片可直接用於測試模型的辨識能力。

## ⚠️ 注意事項

1. **模型相容性**:
   - 本專案使用 **YOLOv7** 架構，模型檔案必須是 YOLOv7 訓練的 `.pt` 檔案
   - 不支援 YOLOv5 或 YOLOv8 模型（架構不相容）

2. **圖片格式建議**:
   - 建議使用清晰的撲克牌花色圖片
   - 圖片尺寸會自動調整為設定的大小（預設 640x640）

3. **影片處理建議**:
   - 較長的影片建議提高取樣率（例如每 60 幀處理一次）以加快處理速度
   - 影片解析度越高，處理時間越長
   - 建議使用 MP4 格式以獲得最佳相容性

4. **辨識效果**:
   - 清晰的花色圖案會有較高的辨識準確率
   - 過於模糊或遮擋的花色可能影響辨識結果

5. **環境相依性**:
   - 確保已正確安裝 `requirements.txt` 中的所有套件
   - 需要 PyTorch >= 1.10.0
   - YOLOv7 需要較新版本的 Python (>= 3.8)
   - OpenCV 用於影片處理，需要系統支援

## 🎓 學習資源

- [YOLOv7 官方 GitHub](https://github.com/WongKinYiu/yolov7)
- [YOLOv7 論文](https://arxiv.org/abs/2207.02696)
- [Streamlit 官方教學](https://docs.streamlit.io/)
- [OpenCV Python 教學](https://docs.opencv.org/4.x/d6/d00/tutorial_py_root.html)
- [PyTorch 官方文檔](https://pytorch.org/docs/stable/index.html)

## 📄 授權

本專案僅供教育與研究用途。

---

**開發者**: 陳淨 | **課程**: 研究方法論 | **日期**: 2025-11
